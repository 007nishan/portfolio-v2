{
  "challenge_number": 61,
  "concepts_text": null,
  "date_id": "2025-10-10",
  "fcc_description": "<p>For the final day of Space Week, you will be given the mass in kilograms (kg) of a payload you want to send to orbit. Determine the amount of fuel needed to send your payload to orbit using the following rules:</p>\n<ul>\n<li>Rockets require 1 kg of fuel per 5 kg of mass they must lift.</li>\n<li>Fuel itself has mass. So when you add fuel, the mass to lift goes up, which requires more fuel, which increases the mass, and so on.</li>\n<li>To calculate the total fuel needed: start with the payload mass, calculate the fuel needed for that, add that fuel to the total mass, and calculate again. Repeat this process until the additional fuel required is less than 1 kg, then stop.</li>\n<li>Ignore the mass of the rocket itself. Only compute fuel needed to lift the payload and its own fuel.</li>\n</ul>\n<p>For example, given a payload mass of 50 kg, you would need 10 kg of fuel to lift it (payload / 5), which increases the total mass to 60 kg, which needs 12 kg to lift (2 additional kg), which increases the total mass to 62 kg, which needs 12.4 kg to lift - 0.4 additional kg - which is less 1 additional kg, so we stop here. The total mass to lift is 62.4 kg, 50 of which is the initial payload and 12.4 of fuel.</p>\n<ul>\n<li>Return the amount of fuel needed rounded to one decimal place.</li>\n</ul>",
  "fcc_js_tests": "[{\"text\": \"<p><code>launchFuel(50)</code> should return <code>12.4</code>.</p>\", \"testString\": \"assert.equal(launchFuel(50), 12.4);\"}, {\"text\": \"<p><code>launchFuel(500)</code> should return <code>124.8</code>.</p>\", \"testString\": \"assert.equal(launchFuel(500), 124.8);\"}, {\"text\": \"<p><code>launchFuel(243)</code> should return <code>60.7</code>.</p>\", \"testString\": \"assert.equal(launchFuel(243), 60.7);\"}, {\"text\": \"<p><code>launchFuel(11000)</code> should return <code>2749.8</code>.</p>\", \"testString\": \"assert.equal(launchFuel(11000), 2749.8);\"}, {\"text\": \"<p><code>launchFuel(6214)</code> should return <code>1553.4</code>.</p>\", \"testString\": \"assert.equal(launchFuel(6214), 1553.4);\"}]",
  "fcc_py_tests": "[{\"text\": \"<p><code>launch_fuel(50)</code> should return <code>12.4</code>.</p>\", \"testString\": \"({test: () => { runPython(`\\nfrom unittest import TestCase\\nTestCase().assertEqual(launch_fuel(50), 12.4)`)\\n}})\"}, {\"text\": \"<p><code>launch_fuel(500)</code> should return <code>124.8</code>.</p>\", \"testString\": \"({test: () => { runPython(`\\nfrom unittest import TestCase\\nTestCase().assertEqual(launch_fuel(500), 124.8)`)\\n}})\"}, {\"text\": \"<p><code>launch_fuel(243)</code> should return <code>60.7</code>.</p>\", \"testString\": \"({test: () => { runPython(`\\nfrom unittest import TestCase\\nTestCase().assertEqual(launch_fuel(243), 60.7)`)\\n}})\"}, {\"text\": \"<p><code>launch_fuel(11000)</code> should return <code>2749.8</code>.</p>\", \"testString\": \"({test: () => { runPython(`\\nfrom unittest import TestCase\\nTestCase().assertEqual(launch_fuel(11000), 2749.8)`)\\n}})\"}, {\"text\": \"<p><code>launch_fuel(6214)</code> should return <code>1553.4</code>.</p>\", \"testString\": \"({test: () => { runPython(`\\nfrom unittest import TestCase\\nTestCase().assertEqual(launch_fuel(6214), 1553.4)`)\\n}})\"}]",
  "fcc_starter_js": "function launchFuel(payload) {\n\n  return payload;\n}",
  "fcc_starter_py": "def launch_fuel(payload):\n\n    return payload",
  "image_path": "20251010_card.jpg",
  "problem_text": null,
  "qa_text": null,
  "quote_text": null,
  "solution_code": null,
  "source": "fcc_api",
  "title": "Space Week Day 7: Launch Fuel"
}
