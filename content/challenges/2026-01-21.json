{
  "challenge_number": 164,
  "concepts_text": null,
  "date_id": "2026-01-21",
  "fcc_description": "<p>Given a string of Markdown that includes one or more inline code blocks, return the equivalent HTML string.</p>\n<p>Inline code blocks in Markdown use a single backtick (<code>`</code>) at the start and end of the code block text.</p>\n<p>Return the given string with all code blocks converted to HTML <code>code</code> tags.</p>\n<p>For example, given the string <code>\"Use `let` to declare the variable.\"</code>, return <code>\"Use &#x3C;code>let&#x3C;/code> to declare the variable.\"</code>.</p>\n<p>Note: The console may not display HTML tags in strings when logging messages. Check the browser console to see logs with tags included.</p>",
  "fcc_js_tests": "[{\"text\": \"<p><code>parseInlineCode(\\\"Use `let` to declare the variable.\\\")</code> should return <code>\\\"Use &#x3C;code>let&#x3C;/code> to declare the variable.\\\"</code>.</p>\", \"testString\": \"assert.equal(parseInlineCode(\\\"Use `let` to declare the variable.\\\"), \\\"Use <code>let</code> to declare the variable.\\\");\"}, {\"text\": \"<p><code>parseInlineCode(\\\"Use `let` or `const` to declare a variable.\\\")</code> should return <code>\\\"Use &#x3C;code>let&#x3C;/code> or &#x3C;code>const&#x3C;/code> to declare a variable.\\\"</code>.</p>\", \"testString\": \"assert.equal(parseInlineCode(\\\"Use `let` or `const` to declare a variable.\\\"), \\\"Use <code>let</code> or <code>const</code> to declare a variable.\\\");\"}, {\"text\": \"<p><code>parseInlineCode(\\\"Run `npm install` then `npm start`.\\\")</code> should return <code>\\\"Run &#x3C;code>npm install&#x3C;/code> then &#x3C;code>npm start&#x3C;/code>.\\\"</code>.</p>\", \"testString\": \"assert.equal(parseInlineCode(\\\"Run `npm install` then `npm start`.\\\"), \\\"Run <code>npm install</code> then <code>npm start</code>.\\\");\"}]",
  "fcc_py_tests": "[{\"text\": \"<p><code>parse_inline_code(\\\"Use `let` to declare the variable.\\\")</code> should return <code>\\\"Use &#x3C;code>let&#x3C;/code> to declare the variable.\\\"</code>.</p>\", \"testString\": \"({test: () => { runPython(`\\nfrom unittest import TestCase\\nTestCase().assertEqual(parse_inline_code(\\\"Use \\\\`let\\\\` to declare the variable.\\\"), \\\"Use <code>let</code> to declare the variable.\\\")`)\\n}})\"}, {\"text\": \"<p><code>parse_inline_code(\\\"Use `let` or `const` to declare a variable.\\\")</code> should return <code>\\\"Use &#x3C;code>let&#x3C;/code> or &#x3C;code>const&#x3C;/code> to declare a variable.\\\"</code>.</p>\", \"testString\": \"({test: () => { runPython(`\\nfrom unittest import TestCase\\nTestCase().assertEqual(parse_inline_code(\\\"Use \\\\`let\\\\` or \\\\`const\\\\` to declare a variable.\\\"), \\\"Use <code>let</code> or <code>const</code> to declare a variable.\\\")`)\\n}})\"}, {\"text\": \"<p><code>parse_inline_code(\\\"Run `npm install` then `npm start`.\\\")</code> should return <code>\\\"Run &#x3C;code>npm install&#x3C;/code> then &#x3C;code>npm start&#x3C;/code>.\\\"</code>.</p>\", \"testString\": \"({test: () => { runPython(`\\nfrom unittest import TestCase\\nTestCase().assertEqual(parse_inline_code(\\\"Run \\\\`npm install\\\\` then \\\\`npm start\\\\`.\\\"), \\\"Run <code>npm install</code> then <code>npm start</code>.\\\")`)\\n}})\"}]",
  "fcc_starter_js": "function parseInlineCode(markdown) {\n\n  return markdown;\n}",
  "fcc_starter_py": "def parse_inline_code(markdown):\n\n    return markdown",
  "image_path": "20260121_card.jpg",
  "problem_text": null,
  "qa_text": null,
  "quote_text": null,
  "solution_code": null,
  "source": "fcc_api",
  "title": "Markdown Inline Code Parser"
}
